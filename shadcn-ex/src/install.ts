

export const install_component = (name: string) => {

    console.log(`install_component ${name}`);
    /*
    const componentPath = path.resolve(__dirname, "templates", component);
      // const destPath = path.resolve(process.cwd(), options.dest, component);

      if (!fs.existsSync(componentPath)) {
        console.error(`Component "${component}" does not exist in the library.`);
        process.exit(1);
      }
     */
}